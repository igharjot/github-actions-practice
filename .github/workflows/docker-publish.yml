name: Docker Publish

on:
    workflow_dispatch:
    #push:
    #    - main

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout Code
              uses: actions/checkout@v4